
import { Outlet } from "react-router-dom"
import Base_Layout from "./Base_Layout"


const Root_Page = () => {
    return (
        <div>
            <Outlet/>
        </div>
    )
}

export default Root_Page
